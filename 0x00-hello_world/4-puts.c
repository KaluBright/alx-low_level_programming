#include <stdio.h>
/**
 * main- Program entry point
 *
 * Return: if no errors, return zero otherwise return non-zero value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	*Return(0);
}
